<resources xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
        <item name="colorControlNormal">@color/color_btn_black</item> <!--toolbar btn color-->
        <!--Customized for no ActionBar-->
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>

    </style>

    <style name="MainActivityTheme" parent="AppTheme">
        <!--全局字体-->
        <item name="android:typeface">sans</item>
        <item name="android:windowLightStatusBar">true</item>
        <!--Customized ripple color
        <item name="android:colorControlHighlight">@color/color_accent_light</item>-->
    </style>

    <style name="BookActivityTheme" parent="AppTheme">
        <!--全局字体-->
        <item name="android:typeface">sans</item>
        <item name="android:windowLightStatusBar">true</item>

    </style>

    <style name="ViewerActivityTheme" parent="AppTheme">
        <!--全局字体-->
        <item name="android:typeface">sans</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="SearchActivityTheme" parent="AppTheme">
        <!--全局字体-->
        <item name="android:typeface">sans</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="LoginActivityTheme" parent="AppTheme">
        <!--全局字体-->
        <item name="android:typeface">sans</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="LauncherActivityTheme" parent="AppTheme">
        <!--全局字体-->
        <item name="android:typeface">sans</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AnimationFade">
        <item name="android:windowEnterAnimation">@anim/popup_fadein</item>
        <item name="android:windowExitAnimation">@anim/popup_fadeout</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <!--Customized Menu style-->
    <style name="OverflowMenuStyle">
        <!-- 是否覆盖锚点，默认为true，即盖住Toolbar -->
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownWidth">wrap_content</item>
        <item name="android:paddingRight">0dp</item>
        <!-- 弹出层背景颜色 -->
        <item name="android:colorBackground">#FFFFFF</item>
        <!-- 弹出层垂直方向上的偏移，即在竖直方向上距离Toolbar的距离，值为负则会盖住Toolbar -->
        <item name="android:dropDownVerticalOffset">0dp</item>
        <!-- 弹出层水平方向上的偏移，即距离屏幕左边的距离，负值会导致右边出现空隙 -->
        <item name="android:dropDownHorizontalOffset">0dp</item>
        <!-- 设置弹出菜单文字颜色 -->
        <item name="android:textColor">#000000</item>
    </style>


    <!--Customized title style for Toolbar-->
    <style name="ToolbarTitleSize" parent="@style/TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="ToolbarCustomStyle" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="colorControlNormal">@color/color_btn_black</item>
    </style>

    <style name="TablayoutTextSize">
        <item name="android:textSize">13dp</item>
    </style>

</resources>
